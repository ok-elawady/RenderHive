# Production overrides — use on cloud/server deployments.
# Removes volume mounts so the built Docker image is used as-is.
# Usage: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d

services:
  api:
    volumes: []

  frontend:
    volumes: []
